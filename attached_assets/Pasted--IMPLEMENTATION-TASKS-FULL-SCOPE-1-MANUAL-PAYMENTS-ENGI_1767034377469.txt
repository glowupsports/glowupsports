🧱 IMPLEMENTATION TASKS (FULL SCOPE)
1️⃣ MANUAL PAYMENTS ENGINE (CORE FEATURE — P0)
Database: payments (REQUIRED)
payments
- id
- academy_id
- player_id
- payer_name (optional)
- amount
- currency
- payment_method (cash | bank)
- payment_date
- received_by (coach_id | admin_id)
- proof_url (nullable)
- status (pending | confirmed | rejected)
- notes (optional)
- created_at
- updated_at

Admin UI — Payments Management (NEW)

Screen: AdminPaymentsScreen.tsx

Admin must be able to:

➕ Create payment

📎 Upload bank proof (image / PDF)

👤 Select player

👥 Select received_by (coach/admin)

📅 Set date & time

🏷️ Set status (pending / confirmed / rejected)

✏️ Edit payment

❌ Reject with reason

🔍 Filter:

date range

coach

player

payment method

status

Rules:

Only confirmed payments count as revenue

Nothing auto-confirms

Coach Payment Registration (LIMITED)

Coaches can:

Register cash received

Upload bank proof

Submit → status = pending

Coaches CANNOT:

Confirm

Edit confirmed payments

Delete records

Admin is final authority.

Definition of Done

Every euro = payment record

Cash & bank fully traceable

No silent edits

No missing history

2️⃣ REVENUE REPORT — REAL & HONEST (P0)
Replace ALL estimated revenue logic

Revenue calculations must use:

SUM(payments.amount WHERE status = 'confirmed')


Revenue report must show:

This week

This month

Pending payments

Cash vs Bank split

Per coach

Per player

Add banner (mandatory):

💡 “Revenue is based on manually recorded payments (cash & bank).
Online payments will be added later.”

3️⃣ AUDIT LOGS (CRITICAL FOR CASH)

Every action must be logged:

Payment created

Payment edited

Status changed

Rejected (with reason)

Court/location changes

Settings changes

Log:

user_id

role

action

before → after

timestamp

Expose via:

AdminAuditLogsScreen.tsx

4️⃣ COURT MANAGEMENT (P0)
New Screen: AdminCourtsScreen.tsx

Admin can:

Add court

Edit court

Disable court

Assign surface

Assign location

Set availability

Backend:

/api/admin/courts

Must be used by scheduling logic

5️⃣ LOCATION MANAGEMENT (P0)
New Screen: AdminLocationsScreen.tsx

Admin can:

Add location

Edit name & address

Assign courts

Set default location

6️⃣ ADMIN COMMUNICATIONS (P0)

Admins must be able to send announcements.

Features

Send message to:

all players

all coaches

selected group

Push + in-app message

One-way (no replies)

Backend:

/api/admin/notifications

Reuse expo-notifications infra

7️⃣ ADMIN SETTINGS — REAL SETTINGS (P1)

Settings must:

Persist

Affect system behavior

Examples:

Default session duration

Default rates

Attendance rules

XP multipliers (if allowed)

Rules:

No setting without effect

No silent failure

8️⃣ BULK ADMIN ACTIONS (P1)

Admin productivity features:

Bulk message

Bulk mark inactive

Bulk export

9️⃣ EXPORTS (P1)

Exports must include:

Payments export (CSV/PDF)

Revenue report

Player list

Coach list

Exports must:

Work on mobile

Be shareable

Have consistent naming

🔐 RBAC RULES (MANDATORY)

Coach:

create payment (pending only)

Admin:

full CRUD

confirm/reject payments

Owner:

read-only finance view

Unauthorized access → 403

🧪 QA CHECKLIST (MUST PASS ALL)

No revenue without payment record

No confirmed payment without admin

Cash & bank separated

Revenue report = sum confirmed payments

All payment changes logged

Courts & locations editable in UI

Admin announcements delivered

Refresh / relog → data persists

No “$0” fake revenue anywhere

🚢 SHIP CRITERIA

If all above is implemented:

✅ Admin Dashboard = 100% MVP-READY
✅ No Stripe required
✅ Business can operate immediately
✅ Owner & admin trust the data

Stripe can be added later without refactoring.

📌 IMPORTANT FUTURE NOTE (DO NOT BUILD NOW)

Stripe integration

Subscriptions

Auto-billing

Payment reminders

Manual payments are the single source of truth for MVP.

END OF PROMPT