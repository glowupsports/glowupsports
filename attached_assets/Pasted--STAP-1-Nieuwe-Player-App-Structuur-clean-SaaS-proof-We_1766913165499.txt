✅ STAP 1 — Nieuwe Player App Structuur (clean & SaaS-proof)

We bouwen niet opnieuw chaos, we bouwen een skelet dat 5 jaar mee kan.

1️⃣ Player App = eigen “mode” in dezelfde app

Je hebt al:

Coach App

(Web)

Player App (half)

👉 We maken dit expliciet:

AppMode = 'coach' | 'player'


Bij login / bootstrap:

API geeft terug: role + academyId

Navigator switcht automatisch

if (user.role === 'player') {
  return <PlayerNavigator />
}


Geen hacks. Geen flags verspreid.

2️⃣ PlayerNavigator (nieuwe root)
❌ GEEN side menu (voor nu)

Side menus = chaos + game gevoel

✅ Bottom Tabs (premium, rustig)
[ Home ] [ Training ] [ Progress ] [ Schedule ] [ Profile ]

Waarom dit klopt

iOS / Android native feeling

1 hand bruikbaar

Makkelijk uitbreidbaar

Ouders begrijpen dit ook

3️⃣ Navigatie-structuur (exact)
PlayerNavigator
 └── PlayerTabs
     ├── HomeStack
     │   └── PlayerHomeScreen
     │
     ├── TrainingStack
     │   ├── TrainingListScreen
     │   └── TrainingDetailScreen
     │
     ├── ProgressStack
     │   └── ProgressOverviewScreen
     │
     ├── ScheduleStack
     │   └── PlayerScheduleScreen
     │
     └── ProfileStack
         ├── PlayerProfileScreen
         └── PlayerSettingsScreen


👉 Geen deep nesting
👉 Elke tab = 1 mentale context

4️⃣ Wat ELKE Player screen automatisch krijgt

Via PlayerLayout (wrapper):

Academy branding (kleur + logo)

Player context

Coach context

SafeArea

Background style (dark, rustig)

<PlayerLayout>
  <ScreenContent />
</PlayerLayout>


👉 Zo hoef je nooit per screen academy-logica te kopiëren.

5️⃣ Globale Player Context (belangrijk)
PlayerContext {
  playerId
  academyId
  coachId
  level
  xp
  glowScore
}


Alles wat je later bouwt:

gebruikt dit

hoeft geen query params

is SaaS-safe

6️⃣ API-regel (heilig)

Elke Player API call:

GET /player/me
GET /player/sessions
GET /player/progress


❌ NOOIT:

/player?academyId=1&playerId=7


👉 Player ziet alleen zichzelf
👉 Academy isolatie is automatisch

7️⃣ Wat we NU bewust NIET doen

Dit is belangrijker dan wat we wél doen:

❌ Quests
❌ Friends
❌ Matches
❌ Rankings
❌ Currency shop

Die komen pas als:

Coach flow stabiel is

Progress Engine V2 live is

Multi-academy bewezen werkt

8️⃣ Definition of Done – Stap 1

Je bent klaar met Stap 1 als:

✅ Player kan inloggen
✅ App opent automatisch Player App
✅ Tabs werken
✅ Elke tab opent een lege maar correcte screen
✅ Academy context werkt
✅ Geen errors / hacks / TODO’s

Nog geen design-perfection. Dat komt in stap 2.

Volgende stap (alleen als jij ja zegt)

👉 Stap 2: Player Home Screen – “Coach speaks, Player sees”
Dat wordt het hart van alles.

Zeg gewoon:
“Ga door met stap 2”

stap 2

Goed. Stap 2 is waar alles samenkomt.
Hier beslissen we of dit een game is, of een professionele performance app die toevallig gamified is.