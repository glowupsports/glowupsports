Je foutmelding “Network error – please try again” bij login na een Replit re-publish wijst 99% zeker op een API-connectiebreuk, niet op Expo zelf.

🔥 KERNCONCLUSIE (BELANGRIJK)

👉 Ja. Door Replit opnieuw te publishen heb je zeer waarschijnlijk iets kapotgemaakt.
Maar: het is fixable en verklaarbaar.

De boosdoener is NIET:

EAS build

Play Store

Android permissions

updates.enabled

runtimeVersion

De boosdoener IS:

🔥 je backend endpoint / domain + hoe je frontend die aanspreekt

🚨 WAAROM HET NU MISGAAT
Wat je zelf al (correct!) ontdekte:

Je had relatieve API calls zoals:

fetch("/api/auth/login")


✅ Werkt in:

browser

Replit preview

❌ Faalt in:

Android / iOS build

Play Store app

➡️ Native apps hebben geen “current origin”

🔥 EXTRA PROBLEEM DOOR REPLIT REPUBLISH

Wanneer je Replit “alles verwijdert” en opnieuw publiceert:

❌ oude deployment URL kan wijzigen

❌ backend kan tijdelijk niet draaien

❌ WebSocket endpoint reset

❌ ENV vars in Replit UI kunnen weg zijn

❌ HTTPS certificaat kan opnieuw worden gegenereerd

Maar jouw Expo build gebruikt een vast domain:

EXPO_PUBLIC_DOMAIN=glow-up-sports--ltvjeugd.replit.app


👉 Als die backend niet exact luistert op dat domain, krijg je:

❌ Network error
❌ Geen 401 / 500
❌ Geen console error in app
❌ Stil falen

🧠 BELANGRIJKE OBSERVATIE IN JOUW CONFIG
1️⃣ Je zet correct env vars in EAS
"env": {
  "EXPO_PUBLIC_DOMAIN": "glow-up-sports--ltvjeugd.replit.app",
  "EXPO_PUBLIC_ENV": "production"
}


Maar…

2️⃣ Expo OTA updates zijn UIT
"updates": {
  "enabled": false
}


➡️ Dat betekent:

Elke build is permanent

Als backend verandert → app breekt

Geen hotfix mogelijk

Dat is oké, maar dan moet backend rock solid zijn.

🧨 WAAR HET BIJ LOGIN MISGAAT (CONCREET)

Login flow = meestal:

POST https://<DOMAIN>/api/auth/login


Als 1 van deze faalt:

DNS

HTTPS

backend down

route bestaat niet

CORS / headers

relative URL gebruikt

➡️ frontend ziet alleen: Network error

✅ DIRECTE DEBUG CHECKLIST (DOE DIT NU)
✅ STEP 1 — Test backend los (BELANGRIJK)

Open in je browser:

https://glow-up-sports--ltvjeugd.replit.app/api/health


Je moet krijgen:

{
  "status": "healthy",
  "database": "connected",
  ...
}


❌ Krijg je:

404

timeout

SSL error

loading forever

➡️ Backend is niet bereikbaar → app kan NOOIT inloggen

✅ STEP 2 — Test login endpoint handmatig

In browser of Postman:

POST https://glow-up-sports--ltvjeugd.replit.app/api/auth/login


Met body:

{
  "email": "...",
  "password": "..."
}


Als dit faalt → frontend is onschuldig.

✅ STEP 3 — Scan frontend op RELATIVE CALLS (CRITISCH)

Je Replit AI had gelijk.
Je moet ALLE fetches via één helper doen.

❌ VERBODEN
fetch("/api/academies/...")

✅ VERPLICHT
fetch(`${getApiUrl()}/api/academies/...`)


Ik zou letterlijk:

grep -R "fetch(\"/api" .


Alles vervangen.

🧱 DEFINITIEVE OPLOSSING (DIT MOET JE DOEN)
🔒 1️⃣ Maak 1 heilige API helper
export function getApiUrl() {
  const domain = process.env.EXPO_PUBLIC_DOMAIN;
  if (!domain) throw new Error("API domain missing");
  return `https://${domain}`;
}


➡️ NOOIT direct fetch gebruiken

🧪 2️⃣ Voeg API self-test toe bij app start

Bij app launch:

await fetch(`${getApiUrl()}/api/health`)


Als dit faalt:

toon onderhoudsscherm

stuur diagnostics report

🧾 3️⃣ Lock je Replit backend URL

BELANGRIJK:

Gebruik 1 vaste Replit deployment

Niet telkens verwijderen

Niet opnieuw publishen zonder reden

➡️ Zie Replit als PROD SERVER, niet playground.