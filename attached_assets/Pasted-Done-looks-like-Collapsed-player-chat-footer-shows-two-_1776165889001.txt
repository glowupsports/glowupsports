Done looks like
Collapsed player chat footer shows two dark pills that perfectly frame the 60px Play circle in the center:

[  preview text  ] (● Play) [^]

Play circle sits centered both horizontally and vertically within the pill row height
Left pill has chat icon + message preview, no red badge
Right pill has chevron-up + optional unread count badge
Gap is exactly centered on the screen (symmetric flex layout)
Deployment: OTA update
JS/TS-only — push via "OTA Push" workflow, no App Store build needed.

Critical: CORRECT FILE
The player home screen renders CoachChatFooter with mode="player" (PlayerNavigator.tsx ~line 829). Edit ONLY client/coach/components/CoachChatFooter.tsx. Do NOT touch client/player/components/PlayerChatFooter.tsx (unused by player app). Do NOT touch any home screen or navigator files.

Key measurements (from SwipeableTabBar.tsx)
Play button: 60×60px circle (centerButton style)
Play button has marginTop: -30 → its vertical center sits at the tab bar top edge
Tab bar height constant: TAB_BAR_HEIGHT = 85
Current pill height: 40px
Vertical alignment calculation
Current: bottom: TAB_BAR_HEIGHT + insets.bottom → Pills' bottom edge = tab bar top edge → Play circle center = tab bar top edge = pills' bottom edge (Play is at the very bottom of pills — not centered)
Fix: bottom: TAB_BAR_HEIGHT + insets.bottom - 20 → Pills' center = tab bar top edge = Play circle center ✓
Changes in client/coach/components/CoachChatFooter.tsx
1. Fix vertical positioning (collapsed state only)
In the inline bottom style of the Animated.View:

// BEFORE
bottom: TAB_BAR_HEIGHT + insets.bottom
// AFTER
bottom: TAB_BAR_HEIGHT + insets.bottom - 20

Only for the collapsed state (!isExpanded && !isFullscreen). The expanded/fullscreen state keeps: TAB_BAR_HEIGHT + insets.bottom + CENTER_BUTTON_PROTRUSION

2. Gap width — already correct at 90px
60px button + 15px breathing room on each side = 90px. No change needed.

3. Layout — already correct
leftPill: flex: 1 — stretches to left
pillGap: width: 90 — fixed center gap
rightSection: flex: 1, alignItems: flex-end — mirrors left, rightPill hugs right edge This ensures the gap is mathematically centered on any screen width.
4. No red/orange badge on left pill
Already removed in previous iteration. Do not re-add unreadBadge to the left pill's chatIconContainer.

After changes
Run the "OTA Push" workflow to deliver to players instantly.