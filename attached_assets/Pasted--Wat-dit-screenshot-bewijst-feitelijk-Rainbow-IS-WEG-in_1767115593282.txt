✅ Wat dit screenshot bewijst (feitelijk)

❌ Rainbow IS WEG in preview

❌ Rainbow BLIJFT in geïnstalleerde APK

✅ OTA update IS correct gepusht

✅ OTA update wordt correct gerenderd in preview

👉 Conclusie:
De APK gebruikt nog de EMBEDDED bundle, niet de OTA bundle.

Dit is géén Expo-bug, géén channel-issue, géén branch-issue.
Dit is Android + embedded update gedrag.

🎯 DE ECHTE OORZAAK (nu 100% zeker)
🔴 Je APK:

start altijd met de embedded JS

downloadt OTA wel

maar past ‘m pas toe bij een echte app-reload

Android doet GEEN echte reload bij “swipe weg”

Daarom:

Preview → direct OTA (hot reload)

APK → blijft oude UI tonen

✅ DE DEFINITIEVE FIX (2 opties)
🟢 OPTIE A — SNELSTE & BETROUWBAARSTE (aanrader)

👉 Force reload wanneer er een update is

Voeg dit toe (bijv. in App.tsx):
import * as Updates from 'expo-updates';
import { useEffect } from 'react';

export default function App() {
  useEffect(() => {
    async function checkUpdate() {
      try {
        const update = await Updates.checkForUpdateAsync();
        if (update.isAvailable) {
          await Updates.fetchUpdateAsync();
          await Updates.reloadAsync();
        }
      } catch (e) {
        console.log('OTA check failed', e);
      }
    }

    if (!__DEV__) {
      checkUpdate();
    }
  }, []);

  return (
    // jouw app
  );
}

Wat dit doet:

✔️ APK checkt OTA bij start

✔️ Als update bestaat → reloadt zichzelf

✔️ Rainbow verdwijnt gegarandeerd

➡️ DIT is hoe veel production apps OTA afdwingen

🟡 OPTIE B — MANUEEL (alleen voor testen)

Op Android:

Instellingen → Apps → Glow Up Sports

Force Stop

Storage → Clear Cache

App openen

⚠️ Swipe weg = NIET genoeg
⚠️ Dit is onbetrouwbaar voor echte users

❌ Waarom OTA “leek” te falen

Omdat Expo:

update downloadt

maar geen reload forceert

en Android cached agressief

Expo dashboard zegt niet:

“Ik gebruik embedded JS”

Maar dat deed hij wél.

🧠 Belangrijke Glow Up Sports les (goud waard)

Je platform is SaaS-achtig.
Voor SaaS geldt:

OTA zonder reload = onbetrouwbaar

Daarom:

kleine updates → auto reload

grote updates → “App is updating…” scherm

native changes → nieuwe APK

🧾 Samenvatting (definitief)

✔️ OTA pipeline = correct
✔️ Channels = correct
✔️ Branches = correct
✔️ Preview = correct
❌ APK = gebruikt embedded bundle
✅ Fix = Updates.reloadAsync() bij update