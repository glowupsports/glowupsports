Exactly right — that's how it should work. The design intent is:

Coach can set location-specific availability: "I'm at Maple 07:00–12:00, at Safa 14:00–20:00" → when a player selects Maple, only Maple slots show
Coach can also set location-agnostic availability (no location pinned): "I'm available 07:00–22:00 anywhere" → those slots should show for every location the player picks
The current code already supports location-specific rows in the availability table — your availability records just happen to all have location_id = null (the "anywhere" option). The client-side filter breaks that by refusing to show "anywhere" slots when a specific location is chosen.

Let me check if the Availability UI actually lets coaches set location-specific hours:


6 actions
Right — and looking deeper, there's a comment in the code that says:

"We don't filter by locationId because coach_availability doesn't have location set"

So there's a gap in the design: the Availability screen currently doesn't let coaches configure per-location hours. The table in the DB supports it (location_id column exists), but the UI doesn't expose it.

Here's the proper design logic:

Coach availability row	Player selects "All"	Player selects "Maple"
location = null (works anywhere)	✅ Show	✅ Show (available everywhere)
location = Maple (Maple only)	✅ Show	✅ Show
location = Safa (Safa only)	✅ Show	❌ Hide (not assigned to Maple)
The fix I already have in Task #520 handles exactly this — null-location slots appear for every location. That's the quick fix and the conceptually correct behavior.

The longer-term enhancement would be allowing coaches to set per-location availability windows in the Availability screen (so a coach could say "I'm only at Maple 7–12, only at Safa 14–20"). That's a bigger piece of work. For now, Task #520 fixes the crashes + the filter + extends your hours — which will make the booking flow actually work. Want me to add a separate future task for per-location availability configuration?