🔥 REPLIT MASTER PROMPT — COACH DASHBOARD (OFFLINE UITGESCHAKELD)

SYSTEM ROLE
You are a senior mobile architect, backend engineer, and QA lead.
Your task is to make the Coach Dashboard ship-safe by explicitly disabling offline functionality instead of attempting offline sync.

Offline must be treated as NOT SUPPORTED in the MVP.

Reliability > features.
No silent failures are allowed.

🎯 GOAL

The Coach Dashboard must be considered 100% MVP-ready under the following rule:

Offline usage is NOT supported.
Any action performed while offline must be blocked, clearly communicated, and logged.

🚫 HARD REQUIREMENT (NON-NEGOTIABLE)

Do NOT attempt to fix or complete offline sync

Do NOT allow any data-modifying action while offline

Do NOT leave any UX ambiguity

Explicit failure is REQUIRED

🧱 TASKS TO IMPLEMENT (STEP BY STEP)
1️⃣ Detect Offline State (GLOBAL)

Implement reliable network detection:

Mobile: @react-native-community/netinfo

Web: navigator.onLine + event listeners

Expose a global isOffline state that can be consumed by:

screens

components

action handlers

2️⃣ Disable ALL Data-Modifying Actions While Offline

When isOffline === true, block:

Attendance marking

Feedback submission

Session creation / edit / cancel

Template creation

Settings save

Any POST / PATCH / DELETE request

Implementation rules:

Disable buttons (not just ignore taps)

Block API calls defensively (server-side guard optional but recommended)

3️⃣ Explicit UX Feedback (NO SILENT FAILURES)

You MUST add:

A) Global Banner (top of Coach Dashboard)

Text (exact or equivalent):

⚠️ Offline mode is not supported yet.
Please reconnect to save changes.

B) Toast / Alert on Action Attempt

When coach tries an action while offline:

“You’re currently offline. This action can’t be saved.”

C) Visual Disabled State

Buttons greyed out

Icons muted

No “loading” spinners when offline

4️⃣ Offline Logging (FOR DEBUGGING)

Every blocked offline attempt must be logged locally and/or sent when online:

Log payload:

user_id

coach_id

screen

action attempted

timestamp

offline = true

Purpose:

measure real offline demand

identify which actions coaches WANT offline later

5️⃣ Hide or Neutralize Offline UI Elements

Audit and update:

offlineSync.ts

SyncStatusIndicator.tsx

Required outcome:

No sync indicator visible

No “pending changes” UI

No illusion that offline sync exists

You may:

remove these from UI

or feature-flag them OFF

6️⃣ Server-Side Safety Net (Recommended)

Add backend validation:

If request mutates data AND client marked offline → reject

Return clear error:

HTTP 409 or 400

message: “Offline actions are not supported”

This prevents edge cases and future regressions.

✅ DEFINITION OF DONE (MUST PASS ALL)

Coach cannot save ANY data while offline

Coach is always informed why an action is blocked

No data loss is possible

No fake “saved” UI states

No sync logic is active or implied

Offline attempts are logged

App behavior is predictable and trustworthy

🧪 QA SCENARIOS (YOU MUST VERIFY)

Turn airplane mode ON → try marking attendance → blocked + message

Offline → try feedback submit → blocked

Offline → try session edit → blocked

Offline → refresh app → banner still visible

Go back online → actions work immediately

No API calls are sent while offline

No local data changes persist after refresh

No sync UI appears anywhere

🚢 SHIP CRITERIA

If all above is implemented:

✅ Coach Dashboard is SHIP-READY
✅ No risk of silent data loss
✅ Offline can be safely added later as v2

📌 IMPORTANT NOTE FOR FUTURE

This implementation MUST make it easy to later:

re-enable offline support

based on real usage data from logs

Do not delete offline code permanently — feature-flag it OFF.

OUTPUT EXPECTATION

Code changes applied cleanly

Clear commit messages

No TODOs left related to offline

No partially working offline behavior

END OF TASK