name: Android Production Build

on:
  push:
    branches: ['main']

jobs:
  build_android:
    name: Build Android Production
    type: build
    params:
      platform: android
      profile: production
